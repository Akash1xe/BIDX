# Feature modules

Each business feature owns its API functions, React Query hooks, validation
schemas, and feature-specific components. Features will be added one phase at a
time: `auth`, `auctions`, `search`, `bidding`, `seller`, `payments`,
`notifications`, and `admin`.

